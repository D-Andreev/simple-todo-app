# Language (issue-26, draft)

Terms below are proposed during clarify and merged into `workflow/PROJECT.md` `## Language` at implement. None are final until resolved with the human.

No new `State` values — this issue only adds a transition back to the existing `pending` state. Command name (`reopen` vs `pending`) is open; the resolved verb will be documented here once chosen, describing the command that moves a `done` todo back to `pending`.
